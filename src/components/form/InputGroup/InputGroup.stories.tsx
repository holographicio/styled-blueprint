import * as React from 'react';
import InputGroup from './';
import { storiesOf } from '@storybook/react';

storiesOf('Form/InputGroup', module).add('default', () => (
  <InputGroup>Click Me</InputGroup>
));
