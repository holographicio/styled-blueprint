import * as React from 'react';
import FormGroup from './';
import { storiesOf } from '@storybook/react';

storiesOf('Form/FormGroup', module).add('default', () => (
  <FormGroup>Click Me</FormGroup>
));
